= form_for @hand do |f|
  - if @hand.errors.any?
    #error_explanation
      %h2= "#{pluralize(@hand.errors.count, "error")} prohibited this hand from being saved:"
      %ul
        - @hand.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
